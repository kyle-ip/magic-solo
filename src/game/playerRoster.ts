/** Compatibility re-exports — player forces live in playerDecks.ts */
export {
  DEFAULT_PLAYER_DECK,
  findTemplate,
  findTemplateByName,
  getPlayerDeck,
  getRoster,
  musterForTurn,
  PLAYER_DECKS,
  PLAYER_ROSTER,
  type PlayerDeckDef,
  type PlayerDeckId,
} from './playerDecks'
