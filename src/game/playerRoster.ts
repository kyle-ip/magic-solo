/** Compatibility re-exports — player forces live in playerDecks.ts */
export {
  DEFAULT_PLAYER_DECK,
  PLAYER_DECKS,
  PLAYER_ROSTER,
  findCardDef,
  findCardDefByName,
  findTemplate,
  findTemplateByName,
  getDeckCards,
  getPlayerDeck,
  getRoster,
  musterForTurn,
  type ConstructedCardDef,
  type PlayerDeckDef,
  type PlayerDeckId,
  type PlayerTemplate,
} from './playerDecks'
